---
description: Layui 提示统一用 layui.msg(text)
alwaysApply: true
---

# Layui 提示约定：统一使用 `layui.msg(text)`

在本项目中，提示弹窗统一使用最简写法：`layui.msg(text)`（只传第一个参数）。

- ✅ 允许：`layui.msg('保存成功')`
- ❌ 禁止：`layui.layer.msg(...)` / `layer.msg(...)`
- ❌ 禁止：给 `layui.msg` 传第二个参数（例如 `{ time: ... }`、`{ icon: ... }` 等）

## 示例

✅ 正确：

```javascript
layui.msg('保存成功');
layui.msg('连接失败');
```

❌ 错误：

```javascript
layui.layer.msg('保存成功');
layer.msg('失败');
layui.msg('失败', { time: 2000 });
```

