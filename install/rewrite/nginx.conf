# EMSHOP Nginx 伪静态 · 复制下面这一段到站点配置里即可
# 规则：不存在的文件/目录 → 转给 index.php，由 Dispatcher 识别 pretty URL
# （宝塔面板：网站 → 设置 → 伪静态 → 粘贴 location 块里面的内容）

location / {
    try_files $uri $uri/ /index.php$is_args$args;
}

